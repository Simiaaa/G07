#获取文件夹
ls -F | grep "/$"> 1.json
awk   -F "/"  '{print $1}' 1.json > 2.json
echo "[" >1.json
echo "  \"folders\": [ " >> 1.json
awk 'BEGIN{count=$(sed -n '$=' 2.json)}{print "    \"" $1 "\"" }NR<$count{print ","}' 2.json
#获取文件
#获取关键行
